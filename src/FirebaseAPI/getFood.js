// import firestore from '@react-native-firebase/firestore';

// export async function getFood(foodsRetrieve) {
//   var foodList = [];
//   var snapshot = await firestore().collection('foods').get()
//   snapshot.forEach((doc) => {
//     foodList.push(doc.data());
//   });

//   foodRetrieve(foodList);
// }
