import React from 'react';
import {
    StyleSheet,
    Text,
    View,
    SafeAreaView,
    Image,
    Dimensions,
    TouchableOpacity,
    ScrollView
} from 'react-native';

const ProfileContent = ({navigation}) => {
    return(
        <Text>Ini Profile Content</Text>
    )
}

export default ProfileContent;
